<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Корневой домен SaaS-платформы
    |--------------------------------------------------------------------------
    |
    | На бесплатном shared-хостинге (SpaceWeb и аналоги) автоматические
    | поддомены часто недоступны. Поэтому витрина магазина живёт на path:
    |   https://{root_domain}/{slug}/catalog
    |
    | Позже владелец сможет подключить свой домен (таблица tenant_domains):
    | тогда витрина откроется на этом домене без префикса slug.
    |
    */

    'root_domain' => env('ATLAS_ROOT_DOMAIN', 'atlascms.ru'),

    /*
    |--------------------------------------------------------------------------
    | Режим адресации витрины
    |--------------------------------------------------------------------------
    |
    | path  — /{slug}/… (по умолчанию, работает без DNS/поддоменов)
    | subdomain — {slug}.{root_domain} (нужны wildcard-DNS и vhost)
    |
    */

    'tenant_routing' => env('ATLAS_TENANT_ROUTING', 'path'),

    /*
    |--------------------------------------------------------------------------
    | Зарезервированные первые сегменты пути
    |--------------------------------------------------------------------------
    |
    | Нельзя использовать как slug магазина — это маршруты платформы.
    |
    */

    'reserved_paths' => [
        'shop',
        'platform',
        'create-shop',
        '1c',
        'login',
        'register',
        'up',
        'livewire',
        'storage',
        'build',
        'vendor',
        'api',
        'admin',
        'filament',
        'css',
        'js',
        'theme.css',
    ],

    /*
    |--------------------------------------------------------------------------
    | Обмен с 1С (CommerceML)
    |--------------------------------------------------------------------------
    |
    | Точка входа:
    |   /{slug}/1c/exchange  — предпочтительно (тенант из path)
    |   /1c/exchange         — тенант определяется по Basic Auth (email владельца)
    |
    */

    'onec' => [
        'route' => '/1c/exchange',
        'file_limit' => 10485760, // 10 МБ
        'storage_dir' => '1c',
        'deactivate_missing' => false,
        'sync_import' => env('EXCHANGE_SYNC', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Темы витрины
    |--------------------------------------------------------------------------
    */

    'themes' => [
        'path' => resource_path('views/themes'),
        'default' => 'default',
    ],
];
