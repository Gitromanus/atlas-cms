<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Корневой домен SaaS-платформы
    |--------------------------------------------------------------------------
    |
    | Витрины магазинов размещаются на поддоменах: shop1.{root_domain}.
    | Также поддерживаются собственные домены клиентов (таблица tenant_domains).
    |
    */

    'root_domain' => env('ATLAS_ROOT_DOMAIN', 'atlascms.ru'),

    /*
    |--------------------------------------------------------------------------
    | Обмен с 1С (CommerceML)
    |--------------------------------------------------------------------------
    |
    | Точка входа обмена: /1c/exchange
    | Логин/пароль обмена задаются отдельно для каждого магазина (в настройках тенанта).
    |
    */

    'onec' => [
        'route' => '/1c/exchange',
        // Максимальный размер загружаемой части файла (байты)
        'file_limit' => 10485760, // 10 МБ
        // Каталог хранения файлов обмена (storage/app/1c/{tenant_slug})
        'storage_dir' => '1c',
        // Автоматически деактивировать товары, отсутствующие в выгрузке (полная синхронизация)
        'deactivate_missing' => false,
        // Обрабатывать import.xml/offers.xml сразу в HTTP-запросе (не требует cron/воркера).
        // Для больших каталогов оставьте false и используйте очередь с воркером или расписанием.
        'sync_import' => env('EXCHANGE_SYNC', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Темы витрины
    |--------------------------------------------------------------------------
    |
    | Папка с темами внутри resources/views.
    |
    */

    'themes' => [
        'path' => resource_path('views/themes'),
        'default' => 'default',
    ],
];